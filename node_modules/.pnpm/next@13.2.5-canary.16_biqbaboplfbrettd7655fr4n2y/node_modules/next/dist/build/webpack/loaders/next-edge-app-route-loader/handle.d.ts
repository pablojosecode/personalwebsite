import { NextConfig } from '../../../../server/config-shared';
import { AppRouteModule } from '../../../../server/future/route-handlers/app-route-route-handler';
declare type HandleProps = {
    page: string;
    mod: AppRouteModule;
    nextConfigOutput: NextConfig['output'];
};
export declare function getHandle({ page, mod, nextConfigOutput }: HandleProps): (request: Request) => Promise<Response>;
export {};
